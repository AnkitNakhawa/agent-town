{
  "name": "@agent-town/client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "clean": "rm -rf dist",
    "dev": "vite --port 5173"
  },
  "dependencies": {
    "@agent-town/shared": "*",
    "phaser": "^3.85.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.21"
  }
}
